int main(void)
{
    char buffer[7];
    int  num;

	INIT();
	LCD_INIT(LCD_DISP_ON_CURSOR_BLINK);
	ADCINIT();

	LED2OFF();
	LED2ON();


	while (1) 
	{
		num=GETADC(7);
        LCD_CLRSCR();
		itoa( num , buffer, 10);
        LCD_PRINT(buffer);
		DELAYMS(100);		
	}
}
