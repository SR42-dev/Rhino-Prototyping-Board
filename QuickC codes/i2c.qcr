#define i2c_servo	0X10

int main(void)
{
	INIT();
	I2C_INIT();
	LED2OFF();
	LED2ON();
	while(1)
	{
		for(int servo=0;servo<4000;servo++) 
		{
			I2C_START(i2c_servo+WRITE);
			I2C_WRITE(2);
			I2C_WRITE(servo>>8);
			I2C_WRITE(servo & 0XFF);
			I2C_STOP();
			DELAYMS(3);
		}
	}
}
