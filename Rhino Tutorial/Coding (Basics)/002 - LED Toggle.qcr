int main(void)
{
	INIT();						//Initialize all selected functions and define varialbels and basic functions
	while(1)						//Infinite loop
		{
		TOGGLELED1();			//If LED1 is on turn it off, if its off turn it on
		DELAYMS(100);		//Wait for 100 miliseconds
		}				// Go to first line in while loop as argument is '1' so its infinite loop
}
